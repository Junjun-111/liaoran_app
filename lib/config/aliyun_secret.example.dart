/// 阿里云 AccessKey 模板。
///
/// 使用方式：复制本文件为 `lib/config/aliyun_secret.dart`，
/// 填入你自己的 AccessKey 后即可（该文件已被 .gitignore 忽略，不会上传 GitHub）。
class AliyunSecret {
  AliyunSecret._();

  static const accessKeyId = '你的AccessKeyID';
  static const accessKeySecret = '你的AccessKeySecret';
}
